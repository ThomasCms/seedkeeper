{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body_content %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('edit_user') }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
