{% extends 'base.html.twig' %}

{% block title %}{{ 'edit.seed'|trans }}{% endblock %}

{% block body_content %}
    <h1>{{ 'edit.seed'|trans }}</h1>

    {{ include('seed/_form.html.twig', {'button_label': 'update'|trans}) }}

    <a href="{{ path('seed_index') }}">{{ 'back.list'|trans }}</a>

    {{ include('seed/_delete_form.html.twig') }}
{% endblock %}
